{
  "name": "teste-api-ebac",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "npx cypress run --browser edge",
    "report": "npx allure serve allure-results",
    "report:generate": "npx allure generate allure-results --output allure-report",
    "report:clean": "rm -rf allure-results allure-report && npx allure generate allure-results --output allure-report",
    "start": "npx serverest",
    "cy:run": "npx cypress run"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cypress": "^13.6.0",
    "joi": "^17.12.0"
  },
  "devDependencies": {
    "allure-commandline": "^2.38.0",
    "allure-cypress": "^3.6.0",
    "cypress-plugin-api": "^2.11.2"
  }
}
